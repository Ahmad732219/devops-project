pipeline {
    agent any

    environment {
        DOCKER_HUB_USER = 'ahmad081'
        APP_NAME = 'devops'
        VERSION = 'v2'
    }

    stages {

        stage('Checkout') {
            steps {
                echo 'Cloning repository...'
                git branch: 'main',
                    url: 'https://github.com/Ahmad732219/devops-project.git'
            }
        }

        stage('Security Scan') {
            steps {
                echo 'Running Trivy security scan...'
                sh 'trivy fs --exit-code 0 --severity HIGH,CRITICAL --no-progress . || true'
            }
        }

        stage('Docker Build Backend') {
            steps {
                echo 'Building backend Docker image...'
                dir('app/backend') {
                    sh "docker build -t ${DOCKER_HUB_USER}/${APP_NAME}-backend:${VERSION} ."
                }
            }
        }

        stage('Docker Build Frontend') {
            steps {
                echo 'Building frontend Docker image...'
                dir('app/frontend') {
                    sh "docker build -t ${DOCKER_HUB_USER}/${APP_NAME}-frontend:${VERSION} ."
                }
            }
        }

        stage('Push to Docker Hub') {
            steps {
                echo 'Pushing images to Docker Hub...'
                withCredentials([usernamePassword(
                    credentialsId: 'dockerhub-credentials',
                    usernameVariable: 'DOCKER_USER',
                    passwordVariable: 'DOCKER_PASS'
                )]) {
                    sh "echo $DOCKER_PASS | docker login -u $DOCKER_USER --password-stdin"
                    sh "docker push ${DOCKER_HUB_USER}/${APP_NAME}-backend:${VERSION}"
                    sh "docker push ${DOCKER_HUB_USER}/${APP_NAME}-frontend:${VERSION}"
                }
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deployment complete - Kubernetes deployment in next phase'
            }
        }
    }

    post {
        success {
            echo 'Pipeline completed successfully!'
        }
        failure {
            echo 'Pipeline failed!'
        }
    }
}
