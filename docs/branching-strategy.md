# Branching Strategy
## Branches
- main: production-ready code only
- dev: integration branch, all features merge here first
- feature/security: security-related features and fixes
## Workflow
1. Create feature branch from dev
2. Open Pull Request to merge into dev
3. After testing, merge dev into main
